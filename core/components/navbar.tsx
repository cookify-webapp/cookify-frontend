import React, { useEffect } from "react";
import { Observer } from "mobx-react-lite";

export const Navbar = () => {
  //---------------------
  // CONTEXT
  //---------------------

  //---------------------
  // EFFECT
  //---------------------
  useEffect(() => {}, []);

  //---------------------
  // RENDER
  //---------------------
  return (
    <Observer>
      {() => (
        <div>
          
        </div>
      )}
    </Observer>
  );
};
